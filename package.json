{
  "name": "slashpad-sidecar",
  "version": "0.1.11",
  "private": true,
  "description": "JS sidecar wrapping the Claude Agent SDK. Spawned by the Rust slashpad binary — not a standalone app.",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.111"
  }
}
